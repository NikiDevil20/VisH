<UserControl x:Class="VisH.View.UserControls.BottomBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:VisH.View.UserControls"
             mc:Ignorable="d" Background="#006DB3"
             d:DesignHeight="300" d:DesignWidth="300"
             DataContext="{Binding DownloadManager, 
    PresentationTraceSources.TraceLevel=High}">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>
    
    <UserControl.Effect>
        <DropShadowEffect
            Direction="90"
            ShadowDepth="3"
            BlurRadius="10">
        </DropShadowEffect>
    </UserControl.Effect>
    
    <Grid >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.4*"/>
            <ColumnDefinition Width="0.4*"/>
            <ColumnDefinition Width="0.2*"/>
        </Grid.ColumnDefinitions>
        
        <TextBlock Grid.Column="0" HorizontalAlignment="Left" VerticalAlignment="Center" Foreground="White" Margin="5, 0"
                   FontSize="14" FontFamily="Arial" Text="{Binding CurrentFileName}"/>
    
        <ProgressBar
            Grid.Column="1"
            Value="{Binding LocalDownloadPercentage}"
            Minimum="0"
            Maximum="100"
            Margin="5"
            Visibility="{Binding IsDownloading, Converter={StaticResource BooleanToVisibilityConverter}}"/>
        
        <TextBlock Grid.Column="2" HorizontalAlignment="Left" VerticalAlignment="Center" Foreground="White" Margin="5, 0"
                   Visibility="{Binding IsDownloading, Converter={StaticResource BooleanToVisibilityConverter}}"
                   FontSize="14" FontFamily="Arial" Text="{Binding DownloadPercentage, StringFormat={}{0:F1} %}"/>
    </Grid>
</UserControl>

