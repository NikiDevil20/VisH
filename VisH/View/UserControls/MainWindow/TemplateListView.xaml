<UserControl x:Class="VisH.View.UserControls.TemplateListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:VisH.View.UserControls" Background="#323232"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="40"></RowDefinition>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <TextBlock Text="Templates" Margin="5" HorizontalAlignment="Center" Foreground="White"
                   FontSize="14"></TextBlock>
        
        
        
        <!-- ListView -->
        <ListView x:Name="TemplateLv" Grid.Row="1" Background="Transparent" BorderBrush="Transparent" 
                  BorderThickness="0" Foreground="White" Margin="5" SelectionMode="Single">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="Margin" Value="0, 0, 0, 10"></Setter>
                </Style>
            </ListView.ItemContainerStyle>
            
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Vertical" Margin="5" VerticalAlignment="Top">
                        <TextBlock Text="{Binding Title}" FontSize="14"></TextBlock>
                        <TextBlock Text="{Binding Description}" FontSize="12"></TextBlock>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
            
        </ListView>
        
        <!-- Footer -->
        <Grid Grid.Row="2" HorizontalAlignment="Stretch" VerticalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition ></ColumnDefinition>
                <ColumnDefinition ></ColumnDefinition>
            </Grid.ColumnDefinitions>
            
            <Button Name="AddTemplateBtn" Grid.Column="0" Content="+" Click="AddTemplateBtn_OnClick"
                    Margin="5, 5, 2.5, 5" Height="30" FontSize="16"></Button>
            <Button Name="RemoveTemplateBtn" Grid.Column="1" Content="-" Click="RemoveTemplateBtn_OnClick"
                    Margin="2.5, 5, 5, 5" Height="30" FontSize="16"></Button>
            
            
            
        </Grid>
        
    </Grid>
</UserControl>

