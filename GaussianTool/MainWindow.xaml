<Window x:Class="GaussianTool.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GaussianTool"
        xmlns:userControls="clr-namespace:GaussianTool.View.UserControls"


        mc:Ignorable="d" WindowStyle="None"
        Title="Gaussian Tool" Height="720" Width="1280">
    <WindowChrome.WindowChrome>
        <WindowChrome GlassFrameThickness="0" CaptionHeight="0"></WindowChrome>
    </WindowChrome.WindowChrome>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="70" MinHeight="70"></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="30" MinHeight="30"></RowDefinition>
        </Grid.RowDefinitions>
        
        <!-- Top Row -->
        <userControls:MenuBar Panel.ZIndex="3" DataContext="{Binding MenuBarViewModel}"/>
        
        <!-- Middle Row -->
        
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200" MinWidth="200" MaxWidth="400"></ColumnDefinition>
                <ColumnDefinition Width="5"></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition Width="200" MinWidth="200"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            
            <!-- Menu Panel -->
            <userControls:TemplateListView Grid.Column="0"></userControls:TemplateListView>
            
            <!-- Splitter -->
            <Border Grid.Column="1" Background="#323232" CornerRadius="0" Panel.ZIndex="2">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="10"
                                      ShadowDepth="3"
                                      Direction="0"
                                      />
                </Border.Effect>
            </Border>
            
            <GridSplitter Grid.Column="1" VerticalAlignment="Stretch" ResizeDirection="Columns" 
                          ResizeBehavior="PreviousAndNext" HorizontalAlignment="Stretch" 
                          Background="Transparent" Panel.ZIndex="2">
            </GridSplitter>
            
            <!-- Content Panel -->
            
            <userControls:Overview Grid.Column="2" DataContext="{Binding OverviewViewModel}"></userControls:Overview>
            
            
            <!-- Status Bar -->
            <userControls:StatusBar Grid.Column="3" Panel.ZIndex="1" DataContext="{Binding StatusViewModel}"></userControls:StatusBar>
            
        </Grid>
        
        
        
        
        <!-- Bottom Row -->
        <userControls:BottomBar Grid.Row="2" Panel.ZIndex="1"></userControls:BottomBar>

        
    </Grid>
</Window>
