<UserControl x:Class="GaussianTool.View.UserControls.TemplateListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:GaussianTool.View.UserControls" Background="#323232"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="40"></RowDefinition>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <TextBlock Text="Templates" HorizontalAlignment="Center" FontSize="14" Margin="5"
                   Foreground="White"></TextBlock>
        
        
        
        <!-- ListView -->
        <ListView x:Name="TemplateLv" Grid.Row="1" Background="Transparent" BorderBrush="Transparent" 
                  BorderThickness="0" Foreground="White" Margin="5" SelectionMode="Single">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="Margin" Value="0, 0, 0, 10"></Setter>
                </Style>
            </ListView.ItemContainerStyle>
            
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Vertical" Margin="5" VerticalAlignment="Top">
                        <TextBlock Text="{Binding Title}"></TextBlock>
                        <TextBlock Text="{Binding Description}"></TextBlock>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
            
        </ListView>
        
        <!-- Footer -->
        <Grid Grid.Row="2" HorizontalAlignment="Stretch" VerticalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition ></ColumnDefinition>
                <ColumnDefinition ></ColumnDefinition>
            </Grid.ColumnDefinitions>
            
            <Button Name="AddTemplateBtn" Content="+" Height="30" Margin="5, 5, 2.5, 5" Grid.Column="0" 
                    Click="AddTemplateBtn_OnClick" FontSize="16"></Button>
            <Button Name="RemoveTemplateBtn" Content="-"  Height="30" Margin="2.5, 5, 5, 5" Grid.Column="1"
                    Click="RemoveTemplateBtn_OnClick" FontSize="16"></Button>
            
            
            
        </Grid>
        
    </Grid>
</UserControl>
